import 'package:flutter/material.dart';

class AdvancedSummaryScreen extends StatelessWidget {
  const AdvancedSummaryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Advanced Summary Screen'),
    );
  }
} 